docker==7.1.0
pydantic>=2.11,<3
httpx>=0.27

# OpenHands V1 Software Agent SDK — embedded as a library, not as a
# subprocess. Replaces the V0 multi-role agents (implementer/reviewer/
# simplifier) with one autonomous loop. Runs locally inside the worker
# process; no Docker-in-Docker. Sandboxing comes from atelier's
# branch_guard + path-aliased worktrees.
openhands-sdk==1.18.1
openhands-tools==1.18.1

# OpenTelemetry pin alignment.
# `lmnr` (transitive dep of openhands-sdk → tracing) hard-pins
# `opentelemetry-semantic-conventions==0.60b1`, while the prefecthq/prefect
# base image ships the matching otel-instrumentation/distro at `0.61b0`.
# pip's resolver gives up. Forcing 0.60b1 across the otel-instrumentation
# family lets lmnr install and downgrades Prefect's telemetry by one beta
# version (atelier doesn't enable Prefect's OTel exporter, so this is
# observably a no-op).
opentelemetry-instrumentation==0.60b1
opentelemetry-instrumentation-logging==0.60b1
opentelemetry-instrumentation-system-metrics==0.60b1
opentelemetry-instrumentation-threading==0.60b1
opentelemetry-semantic-conventions==0.60b1
opentelemetry-distro==0.60b1
opentelemetry-test-utils==0.60b1
